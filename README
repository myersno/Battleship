This is a simple battleship game that runs in terminal via text output. 

It's run by simply running the execute file.

All linkers between header files and main .cpp files should be up to date and functioning, however, there have been linker failures in the past that don't troubleshoot properly.
